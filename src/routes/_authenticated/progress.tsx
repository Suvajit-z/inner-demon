import { createFileRoute } from '@tanstack/react-router';
import { getState } from '@/lib/app-state';
export const Route = createFileRoute('/_authenticated/progress')({component:()=>{const s=getState(); return <main className='p-4 pb-24'><h1 className='text-2xl text-[#C9A84C] font-bold'>Progress</h1><div className='mt-4 animate-pulse rounded-xl h-24 bg-zinc-900'/> <p className='mt-3'>Night misses: {s.missedNights}</p>{s.missedNights===1&&<p className='text-amber-400'>⚠️ You missed last night's report. Miss again and your rank resets.</p>}{s.missedNights>=2&&<p className='text-red-500'>Your demon has gone dormant. The war must be restarted.</p>}</main>}})
